description: |
  Defines a mapping from rgb underglow logical positions to matrix physical positions

compatible: "zmk,underglow-transform"

properties:
  columns:
    type: int
    required: true
  rows:
    type: int
    required: true
  map:
    type: array
    required: true